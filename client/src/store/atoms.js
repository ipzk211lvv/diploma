import {atom} from 'recoil'

export const userState = atom({
    key: 'user',
    default: null
});


export const keyboardState = atom({
        key: 'keyboardState',
        default: null
})

export const usersExerciseState = atom({
        key: 'userExercise',
        default: null
})

